<UserControl x:Class="DoAnWPF.views.QLLH"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Nhập loại sản phẩm -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 10">
            <Label Content="Loại sản phẩm:" VerticalAlignment="Center" Margin="5"/>
            <TextBox x:Name="txtLoaiSP" Width="200" Margin="5"/>
        </StackPanel>

        <!-- Các nút chức năng -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 0 0 10">
            <Button x:Name="btnThemLoai" Content="Thêm" Margin="5" Width="100"/>
            <Button x:Name="btnXoaLoai" Content="Xóa" Margin="5" Width="100"/>
            <Button x:Name="btnSuaLoai" Content="Sửa" Margin="5" Width="100"/>
        </StackPanel>

        <!-- Bảng danh sách loại sản phẩm -->
        <DataGrid Grid.Row="2" x:Name="dtgLoaiSP" AutoGenerateColumns="False" 
                  ColumnWidth="*" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="100"/>
                <DataGridTextColumn Header="Loại sản phẩm" Binding="{Binding DisplayName}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
